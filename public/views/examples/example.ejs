<% var id = Math.round(Math.random() * (1000 - 1) + 1); %>

<h2><%=example.title%></h2>

<% if(example.note) { %>
  <p><%=example.note%></p>
<% } %>

<div>
    <span class="try"><a href="/console?s=<%=encodeURIComponent(example.statement)%>" target="_blank">try me</a></span>
    <pre class="brush: sql" id="<%=id%>"><%=example.statement%></pre>
</div>
